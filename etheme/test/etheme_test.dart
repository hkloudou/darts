import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:etheme/etheme.dart';

void main() {
  test('adds one to input values', () {
    EThemeColor.primary = Colors.accents[0];
  });
}
