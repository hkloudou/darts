import 'package:test/test.dart';

void main() {
  group('Mqtt buffer', () {
    setUp(() {});

    test('type', () {
      var input = "12";
      for (var i = 0; i < input.length; i++) {
        // input.
        print(input.codeUnitAt(i));
      }
    });
  });
}
